# Creuset — HOST-side dependencies (gate, loader, judge, monitor, eval).
#
# The containers install their own, deliberately narrower, sets:
#   agent/requirements.txt                  (LangChain agent + serve.py)
#   sandbox/mock_services/requirements.txt  (FastAPI mock services)
#   deploy/requirements.router.txt          (blue-green router)

# Judge — talks to any OpenAI-compatible endpoint (JUDGE_API_BASE).
openai>=1.30.0

# Attack library: parsing + schema validation
pyyaml>=6.0.1
jsonschema>=4.22.0

# Sandbox harness drives the `docker` CLI via subprocess (no SDK needed).

# CLIs
typer>=0.12.0
rich>=13.7.0
python-dotenv>=1.0.1

# Router, run locally outside Docker (deploy/router.py)
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0

# Evaluation — markdown results table
tabulate>=0.9.0
