# Creuset core
langchain>=0.2.0
langchain-openai>=0.1.0
langchain-community>=0.2.0
litellm>=1.40.0          # provider-agnostic LLM calls (agent + judge)
openai>=1.30.0

# Mock services
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# Sandbox harness
docker>=7.0.0            # Python Docker SDK (for programmatic container management)
pyyaml>=6.0.1            # attack library parsing
jsonschema>=4.22.0       # attack library validation

# Judge / scorer
regex>=2024.5.15         # richer regex for forbidden-pattern checks

# Gate / router
httpx>=0.27.0            # async HTTP for the Python reverse proxy
aiofiles>=23.2.1

# Utilities
python-dotenv>=1.0.1
rich>=13.7.0             # pretty CLI output for gate runs
typer>=0.12.0            # CLI argument parsing

# Evaluation
tabulate>=0.9.0          # results table formatting
